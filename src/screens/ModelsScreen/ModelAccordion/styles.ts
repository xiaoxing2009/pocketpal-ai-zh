import {StyleSheet} from 'react-native';

export const styles = StyleSheet.create({
  accordion: {
    borderRadius: 8,
    height: 55,
  },
  accordionTitle: {
    fontSize: 14,
  },
  accordionDescription: {
    fontSize: 12,
    paddingBottom: 10,
  },
});
